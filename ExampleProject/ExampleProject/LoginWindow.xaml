<Window x:Class="CryptoExchangeClient.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация"
        Height="330"
        Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Вход в систему криптовалютной биржи"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,15"
                   TextWrapping="Wrap"/>

        <TextBlock Grid.Row="1" Text="Email"/>
        <TextBox x:Name="EmailTextBox"
                 Grid.Row="2"
                 Margin="0,5,0,10"
                 Height="28"/>

        <TextBlock Grid.Row="3" Text="Пароль"/>
        <PasswordBox x:Name="PasswordBox"
                     Grid.Row="4"
                     Margin="0,5,0,10"
                     Height="28"/>

        <StackPanel Grid.Row="5" Margin="0,15,0,0">
            <Button Content="Войти"
                    Height="34"
                    Click="LoginButton_Click"/>

           
        </StackPanel>
    </Grid>
</Window>